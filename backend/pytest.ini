[pytest]
# All backend tests live in tests/. Restricting collection also keeps pytest
# away from hails/hails_platform_test.py (a Hails domain module — the
# "Platform Test Hail" fixture builder — whose name matches the *_test.py
# collection pattern), which otherwise collides as a duplicate module name
# during collection.
testpaths = tests
